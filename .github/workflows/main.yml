name: new-ci

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v6.0.3
      - name: build docker image
        run: docker build -t python-flask .
    
